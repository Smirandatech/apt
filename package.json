{
  "name": "apts",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix apts-backend && npm install --prefix apts-frontend",
    "build:backend": "npm run build --prefix apts-backend",
    "build:frontend": "npm run build --prefix apts-frontend",
    "build": "npm run build:backend && npm run build:frontend"
  }
}
